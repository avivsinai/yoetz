#!/bin/bash
# Portable wrapper for agent-browser
# Prefers agent-browser in PATH, falls back to npx
if command -v agent-browser &>/dev/null; then
    exec agent-browser "$@"
else
    exec npx --yes agent-browser "$@"
fi
